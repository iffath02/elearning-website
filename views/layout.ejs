<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <header>
        <nav id="app-name">
          <img src="https://cdn.pixabay.com/photo/2016/03/19/04/40/cap-1266204__340.png" alt="">
          <h1>Learning Loop</h1>
        </nav>
        <nav id="options">
          <% if(isLoggedIn()){ %>
            <p>Welcome <%= currentUser.name %></p>
            <% } %>  
          <button><a href="/">Home</a></button>
          <% if(!isLoggedIn()){ %>
          <button><a href="/login">Login</a></button>
          <button><a href="/users/signup">Signup</a></button>
          <% } %>
          <% if(isLoggedIn()){ %>
            <% if(isAdmin()){ %>
                <button><a href="/courses/new">Create a course</a></button>
            <% } %>  
            <% if(!isAdmin()){ %>
            <button><a href="/users/courses/<%= currentUser.user_id %>">My Courses</a></button>
            <% } %>
            <form action="/sessions" method="post">
              <input type="hidden" name="_method" value="delete">
              <button>Logout</button>
          </form>   
        <% } %> 
        </nav>
    </header>
    <section class="categories">
            <h2>Browse by categories</h2>
            <nav>
            <a href="/courses/category/front-end">Front-end Development</a>
            <a href="/courses/category/big">Big Data</a>
            <a href="/courses/category">Cloud</a>
            <a href="/courses/category/cybersecurity">Cybersecurity</a>
          </nav>
    </section>    
    <%- body %>
  </body>
</html>